# Rag_pipeline_project_v1
Implemented a local RAG pipeline using locally hosted embedding models and LLMs. Used ChromaDB for vector storage and similarity search, enabling efficient document retrieval. Added modular methods for data ingestion, chunking, embedding generation, and querying to improve pipeline maintainability and performance.
